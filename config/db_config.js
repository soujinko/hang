import dotenv from "dotenv";

dotenv.config();

const config = {
  host: "localhost",
  user: "root",
  password: "rhtnwls1541!",
  database: "hang",
  connectionLimit: 30,
  debug: true,
};

export default config;
