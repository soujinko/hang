import { expect } from 'chai'
import supertest from 'supertest'

describe('Register', () => {
  describe('POST api/users/', () => {
    it('should return 201 when completed, else 401',() => {
      
    })
  })
})